// next.config.mjs
const nextConfig = {
    reactStrictMode: true,
    // Otras configuraciones válidas pueden ir aquí.
};

export default nextConfig;

// const optimizeLocales = require('@react-aria/optimize-locales-plugin');

// module.exports = {
//   webpack(config) {
//     config.plugins.push(
//       optimizeLocales.webpack({
//         locales: ['es-ES', 'en-US']
//       })
//     );
//     return config;
//   }
// };
